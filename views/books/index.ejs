<%- include("../partials/layout-start", {title: title}) %>
<%- include("../partials/menu") %>
<h1><%= title %></h1>

<a class="btn btn-sm btn-primary" href="books/create">Добавить запись</a>

<% if(books.length) { %>
    <table class="table table-striped table-sm  mt-3">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Заголовок</th>
            <th scope="col"></th>
        </tr>
        </thead>

        <%- include("../partials/table", {books: books}) %>
<!--          вставить сюда шаблок таблицы!!!-->

    </table>

<% }else { %>
    <h3 class="mt-5">У вас пока нет книг :/</h3>
<% } %>


<%- include("../partials/layout-end") %>